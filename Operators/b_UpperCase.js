name = "Swastika"
console.log(name)
console.log(name.toUpperCase())

/**
 * Example:
 * age = 23
 * console.log(age.toUpperCase())
 * -> Error
 * Because toUpperCase() can only applied with strings
 */