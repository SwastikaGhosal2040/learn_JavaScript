//Predict the output:
/** 
findEvenOdd = (num) => {
    if(num%2==0){
        return "Even"
    }else{
        return "Odd"
    }
}
console.log(findEvenOdd(23)) 

Convert the above code into a single line of code.

*/

typ = (a) => (a % 2 == 0) ? "Even" : "Odd"

console.log(typ(9))

/**
 * Output:
 * Odd
 */