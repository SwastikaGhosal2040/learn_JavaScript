//Predict the output:
/** 
findEvenOdd = (num) => {
    if(num%2==0){
        return "Even"
    }else{
        return "Odd"
    }
}
console.log(findEvenOdd(23)) 

Convert the above code into a single line of code.

*/

typ = (num) => (num % 2 == 0) ? "Even" : "Odd"

console.log(typ(23))

/**
 * Output:
 * Odd
 */
